[pytest]
# The package is run from the repo root rather than pip-installed, so put the
# root on sys.path. Without this, a bare `pytest` cannot import hello_otel
# (`python -m pytest` happens to work, which makes the difference easy to miss).
pythonpath = .
